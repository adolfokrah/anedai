# Claude Agent SDK — the coding agent
ANTHROPIC_API_KEY=

# E2B — cloud sandbox runtime (clone/scaffold, install, run dev server)
E2B_API_KEY=

# GitHub PAT with `repo` scope — used by "Ship" to push branches + open PRs
# (and to create a repo for scratch projects). Optional until you ship.
GITHUB_TOKEN=
